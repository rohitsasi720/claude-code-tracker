{
  "name": "claude-code-tracker",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/dashboard\"",
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/dashboard",
    "start": "npm run start -w apps/api",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
